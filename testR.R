#this is my second git
delta (iris)
head(iris)

getwd()

#create a repository with a code
#go to github and create a new repository

name(name)

#continue with copying the URL adn go to RStudio

# press 
file > new project > version control > git > paste()
